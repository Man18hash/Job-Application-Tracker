import mongoose from 'mongoose'

// Setup for tests
beforeAll(async () => {
  // Additional setup if needed
})

afterAll(async () => {
  // Cleanup if needed
})

beforeEach(async () => {
  // Reset state before each test
})
